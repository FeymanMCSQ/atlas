{
  "name": "atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Automated content pipeline: signals → insights → drafts → published posts",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "npx --yes concurrently -n orchestrator,brain,ingestor,publisher,transcriber -c blue,yellow,magenta,green,cyan \"tsx apps/orchestrator/src/index.ts\" \"tsx apps/content-brain/src/index.ts\" \"tsx apps/feed-ingestor/src/index.ts\" \"tsx apps/publisher/src/worker.ts\" \"tsx apps/transcriber/src/index.ts\"",
    "start:backend": "npm run dev:backend",
    "clean": "rm -rf node_modules apps/*/node_modules apps/*/dist packages/*/node_modules packages/*/dist",
    "clean:dev": "pgrep -f 'tsx.*src/index.ts' | xargs -r kill -9 || echo 'No orphaned workers found'"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "dotenv": "^17.3.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
